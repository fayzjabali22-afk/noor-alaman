# NOOR AL-AMANI SOVEREIGN ARCHITECTURE BLUEPRINT
**[CMD-2026-0730-ARCHITECTURE-BLUEPRINT-104]**

## 1. Project Blueprint (كتالوج الملفات)
هذا الكتالوج يمثل "المسكن" الشرعي لكل نوع من أنواع الملفات. يجب اتباع هذا الهيكل بصرامة لضمان تطبيق مبادئ "الواجهة الغبية" و"فصل الاهتمامات".

```text
src/
 ├── components/       # المكونات الغبية (Dumb UI) - خالية من أي عمليات جلب أو منطق
 ├── services/         # العقل والمنطق (Smart Logic / Firebase / APIs)
 ├── hooks/            # (Custom Hooks) لربط الواجهات بالمنطق
 ├── types/            # تعريفات TypeScript (Interfaces / Types)
 ├── lib/              # دوال مساعدة وأدوات عامة (Utils / Helpers)
 ├── data/             # البيانات الثابتة والوهمية (Static Data)
 └── index.css         # ملفات CSS/Tailwind العالمية
```

## 2. قواعد المسارات (Path Rules) للذكاء الاصطناعي والمطورين
يجب اتباع هيكلية المجلدات التالية بدقة:
- **مكونات UI:** تُوضع حصراً في `src/components`. لا يسمح بإنشاء ملفات منطق (Logic) داخل هذا المجلد.
- **منطق الأعمال والبيانات:** يُوضع حصراً في `src/services`. (مثل استدعاءات Firebase، الجلب الشبكي).
- **الخطافات المخصصة:** تُوضع في `src/hooks` لربط مكونات العرض بخدمات المنطق.
- في حال اقتراح كود جديد، يجب تحديد المسار الكامل للملف قبل الكود لضمان زراعة الملف في مكانه الصحيح (Single Source of Truth).
